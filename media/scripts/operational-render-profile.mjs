import { DEFAULT_TIMELINE_OPTIONS } from "./timeline.mjs";

export const OPERATIONAL_RENDER_PROFILE = "PUBLIC_CANDIDATE";
export const OPERATIONAL_PUBLIC_CANDIDATE_PLAYBACK_RATE = 1.3;

export const OPERATIONAL_PUBLIC_CANDIDATE_TIMELINE_OPTIONS = Object.freeze({
  ...DEFAULT_TIMELINE_OPTIONS,
  playbackRate: OPERATIONAL_PUBLIC_CANDIDATE_PLAYBACK_RATE,
});
