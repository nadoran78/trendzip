# 작업 목록

## 작업 상태

- `READY`: 목적과 우선순위가 정리되어 시작할 수 있다.
- `IN_PROGRESS`: `develop` 또는 작업 브랜치에서 구현 중이다.
- `BLOCKED`: 외부 결정이나 선행 작업이 필요하다.
- `REVIEW`: 구현과 검증이 끝나 병합을 기다린다.
- `DONE`: 기준 브랜치에 병합됐다.

## 작업 선택 규칙

1. 일반적인 단일 작업은 `develop`에서 직접 진행할 수 있다.
2. 병렬·장기·실험 작업은 작업 ID별 `codex/*` 브랜치로 격리한다.
3. 같은 작업을 다른 세션에서 이어갈 때는 기존 작업 위치를 사용한다.
4. `develop`에서 진행하는 `ACTIVE` 작업은 한 번에 하나만 둔다.
5. `ACTIVE`에는 `IN_PROGRESS`, `BLOCKED`, `REVIEW` 작업만 둔다.
6. `READY`는 위에서 아래 순서로 우선하며 최대 5개만 유지한다.
7. 작업을 시작하기 전에 목적, 범위, 제외 범위, 완료 조건과 검증 방법을 확인한다.
8. 세션을 마칠 때 마지막 갱신일, 진행 상황, 다음 행동과 검증 결과를 갱신한다.
9. `DONE`은 `develop` 직접 작업의 완료 또는 작업 브랜치의 `develop` 병합을 뜻한다.
10. 완료 이력은 최근 5개만 남기고 나머지는 Git 로그에서 확인한다.

## ACTIVE

- 없음

## READY

### FE-003 키워드 상세 페이지

- 상태: READY
- 브랜치: 미정
- 다음 행동: FE-002와 공통 UI 경계를 확인한 뒤 상세 범위를 구체화한다.

뜨는 이유, 관련 영상, 최근 추이와 관련 키워드를 상세 API와 연결한다.

### CHORE-002 통합 검증 명령 추가

- 상태: READY
- 브랜치: 미정
- 다음 행동: 빠른 검사와 DB 포함 전체 검사의 실행 범위를 확정한다.

백엔드와 프론트엔드의 빠른 검사 및 전체 검사를 저장소 루트 명령으로 통일한다.

### CHORE-003 GitHub Actions CI 추가

- 상태: READY
- 브랜치: 미정
- 다음 행동: CHORE-002 완료 후 동일한 통합 검증 명령을 CI에 연결한다.

로컬 통합 검증 명령을 pull request와 develop push에서 자동 실행한다.

## LATER

- PWA 및 프론트 배포 마무리
- OpenAPI와 프론트 TypeScript 타입의 계약 자동화
- 외부 API fixture 기반 크롤링 전체 시나리오 테스트
- 프론트 핵심 사용자 흐름 E2E 테스트
- 아키텍처 규칙 자동 검사

## 최근 완료

### FE-002 트렌드 키워드 목록 페이지

- 상태: DONE
- 브랜치: develop
- 완료일: 2026-07-27
- 결과: 확정 시안 기반의 세대별 키워드 랭킹과 공통 헤더를 구현하고, 최신 완료 크롤링 회차의 순위만 반환하도록 목록 API와 캐시 갱신 시점을 보완했다.
- 디자인 차이: `trendScore`는 조회수가 아니므로 트렌드 점수로 표시하고, 집계 기준 시각이 없어 최근 수집 기준이라는 중립 문구를 사용했다. 키워드 상세 이동은 FE-003 범위로 남겼다.
- 배포 메모: 백엔드를 함께 재배포하고 기존 `keywords:v2` Redis 캐시를 한 번 비운다. 비우지 않으면 다음 정상 크롤링 완료 시 자동 갱신된다.
- 검증: 백엔드 전체 테스트·ktlint, 프론트 lint·build, strict 문서 검사와 diff 검사를 통과했다. NEW·UP·DOWN·SAME, null·빈 결과·503 오류와 390x844·320x700·1280x800 화면을 검증했다.

### FE-004 확정 시안 기반 랜딩 페이지

- 상태: DONE
- 브랜치: develop
- 완료일: 2026-07-24
- 결과: `design/app.jsx` 기준의 헤더, 실제 트렌드 티커, 워드마크와 10대·20대 선택 카드를 구현했다. 선택 안내 후 각 세대 피드로 이동하며 API 실패 시에도 랜딩과 이동은 유지된다.
- 디자인 차이: iOS 프레임과 편집 패널은 제외하고 mock 티커는 실제 피드 키워드로 교체했다. 별도 장식용 블러 원 대신 절제된 배경과 노이즈 질감을 적용했다.
- 검증: `npm run lint`, `npm run build`, 문서 검사와 diff 검사를 통과했다. 실제 API와 장애 대체 상태, 390x844·320x700·1280x800 화면 및 TEEN/TWENTY 이동을 검증했다.

### FE-001 실제 피드 조회 및 카드 렌더링

- 상태: DONE
- 브랜치: develop
- 완료일: 2026-07-24
- 결과: 실제 `GET /api/feed` 응답을 유지하면서 `design/feed.jsx` 기준의 세대 전환 헤더, LIVE 상태, 키워드 티커, 오늘의 픽·급상승 섹션과 영상 카드를 구현했다. 영상 새 탭 이동과 로딩·오류·빈 결과·썸네일 누락 상태를 포함한다.
- 디자인 차이: iOS 기기 프레임과 편집 패널은 시안 도구라 제외했다. 랭킹과 키워드 상세는 후속 작업 전까지 비활성 UI로 표시하고 동작 없는 저장 UI는 제외했다.
- 검증: `npm run lint`, `npm run build`, 기본·strict 문서 검사와 diff 검사를 통과했다. 실제 API의 TEEN/TWENTY와 390x844·1280x800 화면, mock 빈 결과·503 오류 화면을 시각 검증했다.

### CHORE-004 프론트 디자인 기준 검증

- 상태: DONE
- 브랜치: develop
- 완료일: 2026-07-24
- 결과: 화면별 디자인 원본을 `design/README.md`에 매핑하고 프론트 작업 규칙에 디자인 확인 절차를 추가했다. 활성 `FE-*` 작업은 확정 디자인 경로를 기록해야 문서 검사를 통과한다.
- 검증: 셸 문법, 기본·strict 문서 검사와 diff 공백 검사를 통과했다.

### CHORE-001 프로젝트 맥락 복구 harness

- 상태: DONE
- 브랜치: codex/chore-001-context-harness
- 완료일: 2026-07-22
- 결과: 프로젝트 상태와 작업 목록을 문서화하고, 현재 브랜치와 연결된 작업 및 Git 상태를 확인하는 `dev/context`와 문서 일관성을 검사하는 `dev/check-context`를 추가했다.
- 검증: 셸 문법, 기본·strict 문서 검사, 현재 브랜치 매핑, 상세 출력과 diff 공백 검사를 통과했다.
