# 작업 목록

## 작업 상태

- `READY`: 목적과 우선순위가 정리되어 시작할 수 있다.
- `IN_PROGRESS`: `develop` 또는 작업 브랜치에서 구현 중이다.
- `BLOCKED`: 외부 결정이나 선행 작업이 필요하다.
- `REVIEW`: 구현과 검증이 끝나 병합을 기다린다.
- `DONE`: 기준 브랜치에 병합됐다.

## 작업 선택 규칙

1. 일반적인 단일 작업은 `develop`에서 직접 진행할 수 있다.
2. 병렬·장기·실험 작업은 작업 ID별 `codex/*` 브랜치로 격리한다.
3. 같은 작업을 다른 세션에서 이어갈 때는 기존 작업 위치를 사용한다.
4. `develop`에서 진행하는 `ACTIVE` 작업은 한 번에 하나만 둔다.
5. `ACTIVE`에는 `IN_PROGRESS`, `BLOCKED`, `REVIEW` 작업만 둔다.
6. `READY`는 위에서 아래 순서로 우선하며 최대 5개만 유지한다.
7. 작업을 시작하기 전에 목적, 범위, 제외 범위, 완료 조건과 검증 방법을 확인한다.
8. 세션을 마칠 때 마지막 갱신일, 진행 상황, 다음 행동과 검증 결과를 갱신한다.
9. `DONE`은 `develop` 직접 작업의 완료 또는 작업 브랜치의 `develop` 병합을 뜻한다.
10. 완료 이력은 최근 5개만 남기고 나머지는 Git 로그에서 확인한다.

## ACTIVE

### CHORE-001 프로젝트 맥락 복구 harness

- 상태: IN_PROGRESS
- 브랜치: codex/chore-001-context-harness
- 시작일: 2026-07-21
- 마지막 갱신: 2026-07-22
- 다음 행동: 검증이 끝난 변경사항을 커밋하고 develop에 병합한다.

#### 목적

여러 Codex 세션에서 작업을 이어가더라도 현재 프로젝트 상태, 활성 작업과 작업 브랜치의 관계를 저장소 안에서 복구할 수 있게 한다.

#### 범위

- 현재 상태와 비즈니스 흐름 문서를 유지한다.
- 작업을 `ACTIVE`, `READY`, `LATER`, `최근 완료`로 구분한다.
- 활성 작업에 상태, 브랜치, 날짜와 다음 행동을 기록한다.
- `dev/context`가 현재 브랜치와 연결된 작업 및 Git 상태를 표시한다.
- `dev/check-context`가 작업 메타데이터, 브랜치 이름과 중복을 검사한다.
- 루트 작업 규칙에 작업별 브랜치 운영과 세션 인계 규칙을 추가한다.

#### 제외 범위

- 브랜치 생성이나 전환을 자동 수행하는 `dev/work`
- 원격 branch fetch 또는 push
- 자동 commit, merge, rebase 또는 branch 삭제
- 통합 빌드 명령과 CI
- 작업 진행률 퍼센트 계산

#### 진행 상황

- [x] 프로젝트 현재 상태 문서화
- [x] 사용자와 크롤링 비즈니스 흐름 문서화
- [x] 기본 `dev/context`와 `dev/check-context` 추가
- [x] 작업별 브랜치 메타데이터와 상태 모델 적용
- [x] 현재 브랜치와 작업 매핑 출력
- [x] 브랜치 규칙과 중복 검사

#### 완료 조건

- ACTIVE 작업 하나가 `develop` 또는 하나의 `codex/*` 브랜치와 연결된다.
- 현재 브랜치와 연결된 ACTIVE 작업을 `./dev/context`에서 확인할 수 있다.
- 기준 브랜치 대비 ahead/behind와 최근 커밋을 확인할 수 있다.
- 연결되지 않은 `codex/*` 브랜치에서 명확한 경고가 출력된다.
- ACTIVE 작업의 필수 메타데이터와 브랜치 중복을 검사한다.
- 조회 명령은 저장소 파일이나 Git 상태를 변경하지 않는다.

#### 관련 코드

- `AGENTS.md`
- `dev/context`
- `dev/check-context`
- `docs/project-status.md`
- `docs/work-items.md`

#### 검증

- 상태: PASS
- 결과: 셸 문법, 기본·strict 문서 검사, 현재 브랜치 매핑, 상세 출력과 diff 공백 검사를 통과했다.

```bash
bash -n dev/context dev/check-context
./dev/check-context
./dev/context
git diff --check
```

#### 인계 메모

기본 맥락 복구 harness는 커밋 `9000564`에 있다. 현재 브랜치에서 작업별 브랜치 인식 기능을 이어서 구현한다.

## READY

### FE-001 실제 피드 조회 및 카드 렌더링

- 상태: READY
- 브랜치: 미정
- 다음 행동: develop에서 직접 시작하고 상세 완료 조건을 확인한다. 병렬 작업이 생기면 `codex/fe-001-feed`로 격리한다.

세대를 선택한 사용자가 백엔드에 수집된 실제 YouTube 피드를 확인할 수 있게 한다.

### FE-002 트렌드 키워드 목록 페이지

- 상태: READY
- 브랜치: 미정
- 다음 행동: FE-001 완료 후 목록 화면의 범위와 완료 조건을 구체화한다.

세대별 급상승 키워드 순위와 변동 정보를 표시하고 키워드 상세로 연결한다.

### FE-003 키워드 상세 페이지

- 상태: READY
- 브랜치: 미정
- 다음 행동: FE-002와 공통 UI 경계를 확인한 뒤 상세 범위를 구체화한다.

뜨는 이유, 관련 영상, 최근 추이와 관련 키워드를 상세 API와 연결한다.

### CHORE-002 통합 검증 명령 추가

- 상태: READY
- 브랜치: 미정
- 다음 행동: 빠른 검사와 DB 포함 전체 검사의 실행 범위를 확정한다.

백엔드와 프론트엔드의 빠른 검사 및 전체 검사를 저장소 루트 명령으로 통일한다.

### CHORE-003 GitHub Actions CI 추가

- 상태: READY
- 브랜치: 미정
- 다음 행동: CHORE-002 완료 후 동일한 통합 검증 명령을 CI에 연결한다.

로컬 통합 검증 명령을 pull request와 develop push에서 자동 실행한다.

## LATER

- PWA 및 프론트 배포 마무리
- OpenAPI와 프론트 TypeScript 타입의 계약 자동화
- 외부 API fixture 기반 크롤링 전체 시나리오 테스트
- 프론트 핵심 사용자 흐름 E2E 테스트
- 아키텍처 규칙 자동 검사

## 최근 완료

- 없음
